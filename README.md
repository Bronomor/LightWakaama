## OWN WAKAAMA IMPLEMENTATION

### Requirements
To run program we need only GCC+11

### Run mock example

We want to add CoAP to LWM2M message. To run program please, follow the commands below
```
mkdir cmake-build-release && cmake-build-release
cmake ..
make
./ultraclient
```